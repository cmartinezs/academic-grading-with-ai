{
  "version": 2,
  "comment": "Explicit allow-list for approved synthetic fixtures, fictional onboarding material and example templates. No wildcard rule entries are permitted and high-confidence rules (private keys, service tokens, .env files) can never be allow-listed.",
  "entries": [
    {
      "path": "engine/c0/fixtures/synthetic/**",
      "rules": ["chilean-rut", "email", "roster-file", "submissions-dir", "results-dir"],
      "reason": "Approved synthetic C0 fixtures: zero real data, reserved example domains only."
    },
    {
      "path": "onboarding/**",
      "rules": ["chilean-rut", "email"],
      "reason": "Fictional onboarding practice material with reserved example domains."
    },
    {
      "path": "automation/ava/config.example.json",
      "rules": ["secret-assignment"],
      "reason": "Example AVA configuration with placeholder values only."
    },
    {
      "path": "**/.env.example",
      "rules": ["secret-assignment"],
      "reason": "Example environment template without real secrets."
    },
    {
      "path": "engine/c0/tests/**",
      "rules": ["chilean-rut", "email"],
      "reason": "Synthetic tests use fictional RUT/email-shaped identifiers; secret detection stays active."
    },
    {
      "path": "engine/publication/tests/**",
      "rules": ["chilean-rut", "email"],
      "reason": "C1 synthetic tests use fictional RUT/email-shaped identifiers in fixtures; secret detection stays active."
    },
    {
      "path": "engine/scripts/publication_snapshot_test.py",
      "rules": ["chilean-rut"],
      "reason": "C1 synthetic E2E self-check uses a fictional RUT-shaped student id."
    },
    {
      "path": "engine/grade_policy/tests/**",
      "rules": ["chilean-rut", "email"],
      "reason": "C2 synthetic tests use fictional RUT/email-shaped identifiers in fixtures; secret detection stays active."
    },
    {
      "path": "engine/email_delivery/transport/smtp.py",
      "rules": ["secret-assignment"],
      "reason": "SMTP transport reads password from environment variable only; variable is never logged, serialized, or committed."
    },
    {
      "path": "engine/email_delivery/tests/**",
      "rules": ["chilean-rut", "email"],
      "reason": "C3 synthetic tests use fictional RUT/email-shaped identifiers in fixtures; secret detection stays active."
    },
    {
      "path": "docs/implementation/C3-*.md",
      "rules": ["secret-assignment"],
      "reason": "C3 documentation references the password variable name descriptively; no actual secrets."
    }
  ]
}

{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://academic-grading-with-ai/schemas/migrations-provenance/1.0.0",
  "title": "Provenance migrations",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "sectionId", "appliedMigrations"],
  "properties": {
    "schemaVersion": { "const": "1.0.0" },
    "sectionId": { "type": "string", "minLength": 1, "maxLength": 64 },
    "appliedMigrations": { "type": "array", "items": { "type": "string" } }
  }
}

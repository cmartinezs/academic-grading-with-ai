"""C1 — Publication Snapshot y lifecycle.

Deterministic core for reproducible, immutable publication snapshots built
from the legacy export adapter. No external effects; no C2 policy engine.
"""

__version__ = "0.1.0"
